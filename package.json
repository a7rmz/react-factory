{
  "name": "react-factory",
  "version": "0.0.0",
  "private": true,
  "description": "React Factory",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean:modules": "rimraf --glob **/node_modules",
    "reinstall": "npm run clean:modules && npm install"
  },
  "workspaces": [
    "packages/*"
  ],
  "volta": {
    "node": "22.14.0"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
