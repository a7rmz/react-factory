{
  "name": "react-factory",
  "version": "0.0.0",
  "private": true,
  "description": "React Factory",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "packages/*"
  ],
  "volta": {
    "node": "22.14.0"
  }
}